@model Fashion__Store.Models.Size

@{
    ViewBag.Title = "Sửa Size";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="main-content flex-grow-1 p-3 p-md-4">
    <div class="card col-md-6 mx-auto shadow-sm">
        <div class="card-header bg-white">
            <h5 class="mb-0">Cập nhật Size</h5>
        </div>
        <div class="card-body">
            @using (Html.BeginForm())
            {
                @Html.AntiForgeryToken()
                @Html.HiddenFor(model => model.SizeId)

                <div class="mb-3">
                    <label class="form-label">Tên Size</label>
                    @Html.EditorFor(model => model.SizeCode, new { htmlAttributes = new { @class = "form-control" } })
                    @Html.ValidationMessageFor(model => model.SizeCode, "", new { @class = "text-danger" })
                </div>

                <div class="mb-3">
                    <label class="form-label">Thứ tự hiển thị</label>
                    @Html.EditorFor(model => model.SortOrder, new { htmlAttributes = new { @class = "form-control", type = "number" } })
                </div>

                @* ĐÃ XÓA PHẦN CHECKBOX ISACTIVE *@

                <div class="d-grid gap-2">
                    <button type="submit" class="btn btn-warning">Cập nhật</button>
                    <a href="@Url.Action("Index")" class="btn btn-light">Hủy bỏ</a>
                </div>
            }
        </div>
    </div>
</div>