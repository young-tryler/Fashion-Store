@model Fashion__Store.Areas.Admin.Models.ProductViewModel

@{
    Layout = "~/Views/Shared/_Layout.cshtml";
    ViewBag.Title = "Thêm mới Sản phẩm";
}

<div class="main-content flex-grow-1 p-3 p-md-4">
    <div class="card shadow-sm border-0">
        <div class="card-header bg-white">
            <h5 class="mb-0">Thêm mới Sản phẩm</h5>
        </div>
        <div class="card-body">
            @using (Html.BeginForm("Create", "Product", FormMethod.Post, new { enctype = "multipart/form-data" }))
            {
                @Html.AntiForgeryToken()

                <div class="row">
                    <div class="col-md-8">
                        <div class="mb-3">
                            @Html.LabelFor(m => m.ProductName, new { @class = "form-label" })
                            @Html.TextBoxFor(m => m.ProductName, new { @class = "form-control" })
                            @Html.ValidationMessageFor(m => m.ProductName, "", new { @class = "text-danger" })
                        </div>

                        <div class="row">
                            <div class="col-md-6 mb-3">
                                @Html.LabelFor(m => m.SKU, new { @class = "form-label" })
                                @Html.TextBoxFor(m => m.SKU, new { @class = "form-control" })
                                @Html.ValidationMessageFor(m => m.SKU, "", new { @class = "text-danger" })
                            </div>
                            <div class="col-md-6 mb-3">
                                @Html.LabelFor(m => m.BasePrice, new { @class = "form-label" })
                                @Html.TextBoxFor(m => m.BasePrice, new { @class = "form-control", type = "number" })
                            </div>
                        </div>

                        <div class="mb-3">
                            @Html.LabelFor(m => m.Slug, new { @class = "form-label" })
                            @Html.TextBoxFor(m => m.Slug, new { @class = "form-control", placeholder = "Để trống để tự tạo từ tên" })
                        </div>

                        <div class="mb-3">
                            @Html.LabelFor(m => m.Description, new { @class = "form-label" })
                            @Html.TextAreaFor(m => m.Description, new { @class = "form-control", rows = "4" })
                        </div>
                    </div>

                    <div class="col-md-4">
                        <div class="mb-3">
                            <label class="form-label">Chọn Danh mục</label>
                            @* Dropdown chọn nhiều danh mục *@
                            @Html.ListBoxFor(m => m.SelectedCategoryIds, (MultiSelectList)ViewBag.Categories, new { @class = "form-select", style = "height: 150px;" })
                            <small class="text-muted">Giữ phím Ctrl để chọn nhiều</small>
                        </div>

                        <div class="mb-3">
                            @Html.LabelFor(m => m.PrimaryImage, new { @class = "form-label" })
                            <input type="file" name="PrimaryImage" class="form-control" />
                        </div>

                        <div class="form-check mb-3">
                            @Html.CheckBoxFor(m => m.IsActive, new { @class = "form-check-input" })
                            @Html.LabelFor(m => m.IsActive, new { @class = "form-check-label" })
                        </div>

                        <button type="submit" class="btn btn-primary w-100">Lưu Sản phẩm</button>
                        <a href="@Url.Action("Index")" class="btn btn-outline-secondary w-100 mt-2">Quay lại</a>
                    </div>
                </div>
            }
        </div>
    </div>
</div>